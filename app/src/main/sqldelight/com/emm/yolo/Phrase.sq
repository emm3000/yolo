CREATE TABLE phrase (
    id INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,

    text TEXT NOT NULL,

    context TEXT NOT NULL,
    -- DAILY, MEETING, INTERVIEW, TECH, GENERAL

    status TEXT NOT NULL,
    -- NEW, PRACTICING, MASTERED

    last_practiced_at INTEGER,
    notes TEXT,

    created_at INTEGER NOT NULL
);


selectAll:
SELECT * FROM phrase
ORDER BY status, created_at DESC;

selectByStatus:
SELECT * FROM phrase
WHERE status = ?;

updateStatus:
UPDATE phrase
SET
    status = ?,
    last_practiced_at = ?
WHERE id = ?;

deleteById:
DELETE FROM phrase WHERE id = ?;

insertPhrase:
INSERT INTO phrase (
    text,
    context,
    status,
    notes,
    created_at
) VALUES (?, ?, ?, ?, ?);

getPhrasesByStatus:
SELECT *
FROM phrase
WHERE status = ?
ORDER BY created_at DESC;

updatePhraseStatus:
UPDATE phrase
SET status = ?, last_practiced_at = ?
WHERE id = ?;
